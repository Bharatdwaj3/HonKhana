apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: catalog
  name: catalog
spec:
  ports:
    - name: "4001"
      port: 4001
      targetPort: 4001
      nodePort: 31001
  selector:
    io.kompose.service: catalog
status:
  loadBalancer: {}
